-- Este archivo se ejecuta solo cuando el volumen está vacío (primera vez)
-- TypeORM con synchronize:true creará las tablas automáticamente al arrancar el backend

SELECT 'Base de datos Docker-Tesis inicializada correctamente' AS mensaje;